1.CREATE VIEW V_HEADER AS
  SELECT * FROM ROUTE_HEADER;

SELECT * FROM TAB;

2.UPDATE V_HEADER
  SET FARE = 42 WHERE ROUTE_ID=102;

SELECT * FROM V_HEADER;

3.CREATE VIEW VRR_ROUTE_HEADER AS
  SELECT ROUTE_HEADER.ROUTE_ID,
  ROUTE_HEADER.ORIGIN,ROUTE_HEADER.DESTINATION,
  ROUTE_HEADER.FARE,ROUTE_DETAILS.PLACE_ID,
  NONSTOP
  FROM ROUTE_HEADER,ROUTE_DETAILS;

SELECT * FROM VRR_ROUTE_HEADER;

4.CREATE SYNONYM TICK
  FOR TICKET_HEADER;

5.CREATE SEQUENCE ticseq
  START WITH 1
  MINVALUE 1
  MAXVALUE 20
  INCREMENT BY 1
  CACHE 10
  CYCLE;

6.ALTER SEQUENCE ticseq
  MAXVALUE 25;

SELECT SEQUENCE_NAME FROM USER_SEQUENCE;

7.CREATE INDEX TICKIN
  ON TICKET_HEADER(ADULTS);

SELECT INDEX_NAME FROM USER_INDEXES WHERE TABLE_NAME = 'TICKET_HEADER'


